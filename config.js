/**
 * config.js
 * Front-end configuration constants shared by all modules.
 */

// Base URL of the Flask API server
const API_BASE = 'http://localhost:5000';

// TC email list (mirrors backend — used only for client-side demo toggle)
const TC_EMAILS = ['njain@abc.com'];

// Tech Council mail alias (used in Ask a Question tab)
const TC_CONTACT_EMAIL = 'techcouncil@abc.com';
